__pycache__/
*.pyc
.DS_Store
*.tmp

.playwright-mcp/

# Hayes' semantic-agents source doc lives in Downloads; excluded from this branch by request
SemanticAgents/

# isolated venv for the vendored Q-Models Explorer (local-cpu endpoint)
.qm-venv/
RohanOnly/moat/

# Chainlit runtime artifacts (local data layer session storage, generated chainlit.md cache)
frontend/.files/
.files/

# Private local-only dir: moat DB, engagements, EMET tester creds + authenticated profile. NEVER commit.
RohanOnly/
**/emet_creds.env
**/emet_profile/
