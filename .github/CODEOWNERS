# Code owners for Sapphire (rohanaryagondi/Sapphire).
# Every PR routes to the owner below for review.
# Rohan is the sole approver/merger for main (see dev/CONTRIBUTORS.md + dev/PR_REVIEW.md).
# Hard server-side "required review" needs GitHub Pro (branch protection); on the free plan
# this routes review by convention. The branch rules are additionally enforced by the
# client-side hooks (.githooks/). A server-side branch-guard Action is staged in dev/ci/
# (Actions can't run on the free private repo yet). See dev/CONTRIBUTOR_RULES.md.

* @rohanaryagondi
